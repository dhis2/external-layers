{
  "name": "external-layers",
  "description": "External layers for DHIS2 Maps",
  "version": "0.1.0",
  "scripts": {
    "validate": "node scripts/validate.js",
    "validate-push": "yarn test"
  },
  "dependencies": {
    "ajv": "^8.12.0"
  }
}
